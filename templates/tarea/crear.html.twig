{% extends 'base.html.twig' %}

{% block title %}Crear tarea{% endblock %}

{% block body %}

<div class="example-wrapper">
    {% if edit is defined%}
        <h2>Editar tarea</h2>
    {% else %}
        <h2>Crear tarea</h2>
    {% endif %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}